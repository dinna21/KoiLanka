import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "KoiCareLanka - Sri Lanka's Premier Koi Community",
  description: "Join Sri Lanka's most trusted network of breeders, collectors, and koi enthusiasts. Discover premium Japanese koi bred in Sri Lankan waters.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
